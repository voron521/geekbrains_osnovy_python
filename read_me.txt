1) ок
2) ок
3) ок
4) сделал, но не понял почему не работал цикл while number[i]: (т.е пока элемент строки существует), пришлось делать while i < len(number):
5) ок
6) в задании не точность, я сделал так, что общий километраж за все дни достигнет результата b на такой-то день. Если надо было посчитать
на какой день спортсмен пробежит, за конкретно этот день b километров, тогда будет формула a += ((a * 10) / 100), а не a += (a + ((a * 10) / 100))